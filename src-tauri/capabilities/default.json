{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities: the renderer only uses invoke (custom commands) and event listen; all plugin and window APIs are called from Rust.",
  "windows": ["main", "floating"],
  "permissions": ["core:default"]
}
